# Changelog

## 0.3.4 - 2026-08-29

- 修复长音频检查点指纹未覆盖静音策略、预检后端、VAD 严格度、设备、精度和 Attention 后端的问题；配置变化时不再复用旧转写，而是安全地从头处理并报告 `configuration_changed`。
- 修复边界去重在分片没有重叠时仍可能删除真实重复短句的问题；现在只会在实际音频重叠区间且两个片段的时间范围确实重合时去重。
- 修复长音频合并后丢失顶层语音预检比例、导致纯噪声或无语音幻觉可能被质量门判为可用的问题。
- 修复 `part001:S01` 跨分片映射无法命中合并后的 `S001001` 说话人编号，并修正节点默认示例。
- 模型缓存身份加入文件指纹并清退同路径的旧权重，避免原地更新模型后继续复用旧缓存或额外占用显存。
- 修复未触发自动重试时进度总量仍按两次推理计算的问题；字幕导出文件名增加随机唯一后缀，避免并发覆盖。
- Registry 发布包排除开发专用构建/校验脚本，并移除发布包中的直接环境变量读取命中；手动发布重试不再默认发布 `main`，缺少 Registry 密钥时会明确失败。
- 修正中英文 Attention 提示，使其与 `auto` 实际执行的 FlashAttention-2、SDPA、eager 显式回退顺序一致。
- 增加断点配置失配、重叠去重、语音预检聚合、跨分片映射、模型缓存、进度、唯一文件名和双语提示的回归测试。

## 0.3.3 - 2026-08-28

- 跟进 OpenMOSS `cb765f2b0fe6f7a298aa2002e2281ae693d1f3c3`：默认 `auto` 不再把 Attention 选择留给 Transformers，而是按 FlashAttention-2、SDPA、eager 顺序显式尝试，避免长音频静默落到平方显存增长的 eager。
- 自动回退会记录跳过、失败和最终选择；用户显式指定的后端仍保持严格语义，失败时不会静默换成其他实现。
- Registry 发布改为在标签 Release 流水线内直接执行，消除由 `GITHUB_TOKEN` 创建 Release 后无法级联触发第二个工作流的问题；独立工作流保留为指定 tag/branch 的手动重试入口。
- 增加每周最新版 ComfyUI 兼容检查，并在 Actions 摘要中记录实际测试的 ComfyUI commit SHA。
- Registry 元数据补充 Windows、Linux 和 NVIDIA CUDA 分类；Python 3.10 的 Tomli 最低版本统一提升到 2.4.1。

## 0.3.2 - 2026-08-26

- 移除会触发 2 个高危、1 个中危公告的 Transformers 4.57.6 可选修复固定，改为安全修复版 Transformers 5.15.1。
- 仍保留 Transformers 4.52.1–5.4 的运行时兼容测试，但环境检查会明确提示已公开安全公告并建议升级；推荐安全范围调整为 `>=5.5.0,<6`。
- 仓库启用 Dependabot 漏洞告警、自动安全更新与不可变 Release；发布流水线先上传 Draft 附件再公开，并升级构建证明 Action。
- 节点运行时、模型权重与固定上游 revision 保持不变。

## 0.3.1 - 2026-08-26

- 修正 v0.3.0 中无法解析的 OpenMOSS 审计基线 SHA，固定到真实上游提交 `cde3c13af82c3001a21cf085d37ebc7d81e8981d`。
- 增加 GitHub 与 Hugging Face 固定 revision 的远程可解析性检查，避免仅靠硬编码测试放过无效溯源信息。
- 增加确定性 Release 打包、重复构建一致性检查、SHA-256 清单、构建清单与 GitHub artifact attestation。
- 发布工具兼容项目声明支持的 Python 3.10，并纳入兼容矩阵验证。
- 增加 Dependabot 与 CodeQL 安全自动化；运行时功能和模型权重保持不变。

## 0.3.0 - 2026-08-26

- 增加智能长音频节点：VAD 静音边界分片、可配置重叠、边界近重复去除、全局时间轴、分片说话人命名空间、原子检查点和中断续跑。
- 能量预检升级为 WebRTC VAD；增加无语音/音乐噪声风险诊断、转写质量门，以及格式或质量异常时的一次严格提示自动重试。
- 使用 ComfyUI V3 原生进度接口并保留旧版回调兼容；新增可复用 ASS 字幕样式节点，支持分辨率、字体、布局、描边、阴影与说话人颜色。
- 增加完整 `en`/`zh` 节点翻译，默认仍为中文；UI/API 示例升级到长音频、质量门和字幕样式链路。
- 审计 OpenMOSS `cde3c13af82c3001a21cf085d37ebc7d81e8981d` 的 Attention 回退提交：`auto` 跟随上游默认，显式后端失败时不静默降级。
- 扩展自动化测试和兼容 CI，并增加 Comfy Registry 发布工作流。

## 0.2.1 - 2026-08-24

- 模型加载器增加显式 Attention 后端选项，自动模式按 Flash Attention、SDPA、eager 顺序审计回退，并把实际选择写入转写元数据和环境报告。
- 缺少 `[Sxx]` 的有效时间戳片段不再丢失，改以未知说话人 `S00` 保留并输出质量告警。
- 模型下载增加磁盘空间预检和仅保存在本机的断点字节、固定 revision、镜像主机与哈希状态报告。
- 代码审计基线更新到 OpenMOSS `e607537b1b870475e7898969d40b864de8b691b6`，模型权重 revision 保持不变。

## 0.2.0 - 2026-08-21

- 增加 `keep`、`release_under_pressure`、`release_after_run` 三档显存驻留策略，并保留旧 `release_after_run` 工作流兼容输入。
- 增加会议、访谈、精简字幕、多语言等提示词预设与静音能量预检；`reject` 在加载模型前终止。
- 字幕导出增加人工跨分片说话人映射，不把局部 `S01/S02` 自动误认成同一人。
- 显存协调不再全局卸载无关 ComfyUI 模型，只清理软缓存并管理本节点自己的模型缓存。
- 更新 UI/API 示例工作流及专项回归测试。

## 0.1.1 - 2026-08-20

- 修复字幕导出和环境释放节点未声明为 ComfyUI 输出节点、示例工作流无法可靠入队的问题。
- 修复模型下载脚本把权重静默放到加载器不扫描目录的问题，并补 Windows Portable 命令。
- 在模型加载前提供 Transformers 版本诊断，并增加 Transformers 4.x/5.x CI 矩阵。
- 增强早停、重复文本和近静音幻觉诊断；协调 ComfyUI 显存并规范化模型缓存键。
- 增加真实输出节点资格、下载目标和缓存精度回归测试。
- 模型损坏修复改用独立 staging 目录，完整校验后再替换目标；中断不会覆盖原目录。
- 示例工作流的 Registry 节点版本同步为 0.1.1，并将最低支持的 Transformers 4.52.1 纳入 CI。

## 0.1.0 - 2026-08-20

- 首次公开版本，提供六个 `comfy_api.latest` V3 节点。
- 支持标准 ComfyUI `AUDIO`、16kHz 安全重采样、透传音频和本地 BF16/FP16/FP32 推理。
- 支持 Transformers 4.52.1 至 5.x，不自动替换 ComfyUI 的 Torch/Transformers 环境。
- 增加固定模型 revision、文件大小/SHA-256 校验和断点下载。
- 增加提示词/热词、转写校验、JSON/TXT/SRT/ASS 导出、说话人重命名、环境诊断和显存释放。
- 增加 ComfyUI 进度回传、队列中断、模型缓存和同模型推理锁。
- 提供 UI/API 基础与长音频诊断工作流。
- 通过独立目录导入、Comfy Registry、本地 GPU 2 分钟和 5 分钟冒烟测试。
