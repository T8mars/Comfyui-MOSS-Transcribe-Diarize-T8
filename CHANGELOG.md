# Changelog

## 0.2.1 - 2026-08-24

- 模型加载器增加显式 Attention 后端选项，自动模式按 Flash Attention、SDPA、eager 顺序审计回退，并把实际选择写入转写元数据和环境报告。
- 缺少 `[Sxx]` 的有效时间戳片段不再丢失，改以未知说话人 `S00` 保留并输出质量告警。
- 模型下载增加磁盘空间预检和仅保存在本机的断点字节、固定 revision、镜像主机与哈希状态报告。
- 代码审计基线更新到 OpenMOSS `e607537b1b870475e7898969d40b864de8b691b6`，模型权重 revision 保持不变。

## 0.2.0 - 2026-08-21

- 增加 `keep`、`release_under_pressure`、`release_after_run` 三档显存驻留策略，并保留旧 `release_after_run` 工作流兼容输入。
- 增加会议、访谈、精简字幕、多语言等提示词预设与静音能量预检；`reject` 在加载模型前终止。
- 字幕导出增加人工跨分片说话人映射，不把局部 `S01/S02` 自动误认成同一人。
- 显存协调不再全局卸载无关 ComfyUI 模型，只清理软缓存并管理本节点自己的模型缓存。
- 更新 UI/API 示例工作流及专项回归测试。

## 0.1.1 - 2026-08-20

- 修复字幕导出和环境释放节点未声明为 ComfyUI 输出节点、示例工作流无法可靠入队的问题。
- 修复模型下载脚本把权重静默放到加载器不扫描目录的问题，并补 Windows Portable 命令。
- 在模型加载前提供 Transformers 版本诊断，并增加 Transformers 4.x/5.x CI 矩阵。
- 增强早停、重复文本和近静音幻觉诊断；协调 ComfyUI 显存并规范化模型缓存键。
- 增加真实输出节点资格、下载目标和缓存精度回归测试。
- 模型损坏修复改用独立 staging 目录，完整校验后再替换目标；中断不会覆盖原目录。
- 示例工作流的 Registry 节点版本同步为 0.1.1，并将最低支持的 Transformers 4.52.1 纳入 CI。

## 0.1.0 - 2026-08-20

- 首次公开版本，提供六个 `comfy_api.latest` V3 节点。
- 支持标准 ComfyUI `AUDIO`、16kHz 安全重采样、透传音频和本地 BF16/FP16/FP32 推理。
- 支持 Transformers 4.52.1 至 5.x，不自动替换 ComfyUI 的 Torch/Transformers 环境。
- 增加固定模型 revision、文件大小/SHA-256 校验和断点下载。
- 增加提示词/热词、转写校验、JSON/TXT/SRT/ASS 导出、说话人重命名、环境诊断和显存释放。
- 增加 ComfyUI 进度回传、队列中断、模型缓存和同模型推理锁。
- 提供 UI/API 基础与长音频诊断工作流。
- 通过独立目录导入、Comfy Registry、本地 GPU 2 分钟和 5 分钟冒烟测试。
