# Extra dependencies only. Never replace the PyTorch stack supplied by ComfyUI.
av>=14.0
filelock>=3.13
huggingface-hub>=0.34,<2
librosa>=0.11.0
numba>=0.61.0
numpy>=1.26,<3
packaging>=24
safetensors>=0.6.2
soundfile>=0.12
soxr>=0.5
tomli>=2.4.1; python_version < "3.11"
webrtcvad-wheels>=2.0.14,<3
# Transformers is intentionally not changed by this file. The node supports
# 4.52.1 <= version < 6; use scripts/check_transformers.py before choosing the
# optional pinned v4 requirements file.
